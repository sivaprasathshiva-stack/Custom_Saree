import { StudioShell } from "@/components/studio/studio-shell";

export const metadata = {
  title: "Textile Studio — SĀRĪ Studio",
};

export default function StudioPage() {
  return <StudioShell />;
}
